{
  "name": "twitch-multi-player-reactive",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate:schema": "npx openapi-typescript oas/twitch.yml --output lib/src/types/schema-twitch.ts",
    "serve:bff": "ts-node -r tsconfig-paths/register bff/src/index.ts",
    "serve:callback": "CALLBACK_ENDPOINT=$(curl -s localhost:4040/api/tunnels | jq -r '.tunnels[].public_url' | grep -e 'https.*') ts-node -r tsconfig-paths/register callback/src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vue/cli": "^4.5.12",
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pusher": "^5.0.0",
    "ts-node": "^9.1.1",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.2.4",
    "uninstall": "0.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "openapi-typescript": "^3.2.4",
    "prettier": "^2.2.1"
  }
}
